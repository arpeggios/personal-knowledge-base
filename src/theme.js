import { createTheme } from "@mui/material";

export const theme = createTheme({
  typography: {
    h1: {
      fontSize: "3rem",
      margin: "1rem auto"
    },
    h2: {
      fontSize: "2rem",
      margin: "1rem 0"
    }
  }
})